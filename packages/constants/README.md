# @my-bahrain/constants (planned)

Add this package **only** for non-business constants:

- Route names / path segments
- React Query keys
- Async storage keys

Do **not** put fees, statuses, limits, or feature flags here — those belong in the Convex `settings` table.
